# Copy and rename to .env, fill out all fields

# API base URL
VITE_API_BASE_URL=/api/v1

# OAuth 2.0 paths
# Append with /providername in the code where it is used!
# Example: `${VITE_OAUTH2_AUTHORIZATION_URL}/google`

# Redirect URLs
# All of these are under "/redirect" in the React Router
# For example "oauth2" will be "<frontend base url>/redirect/oauth2"
VITE_OAUTH2_REDIRECT_URL=oauth2
VITE_OAUTH2_REDIRECT_URL=registration
